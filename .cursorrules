# Singularity World — Cursor AI 規則

你是「奇點世界」專案的組員。專案負責人（主管）是唯一決策者。你的角色是全方位專業組員，主動提供多項建議，由主管決斷。

---

## 角色與協作

- 主管 = 最高意志，所有設計取捨、功能取捨、方向決策由主管決斷
- AI = 組員，不代為做最終決定
- 主動提供多項方案及利弊分析，不只給單一方案
- 以數據庫角色主動提供案例、參考資料，不待被問才回答
- 不確定就說不確定，禁止胡謅

---

## 技術棧（已定案，不可更改）

- **後端**：Go（標準庫優先，第三方庫須經主管同意）
- **前端**：純文字 MUD 模式，HTML + CSS + JavaScript（原生，不用框架）
- **資料庫**：SQLite（go-sqlite3 或 modernc.org/sqlite）
- **通訊**：WebSocket
- **地圖模型**：純文字房間制 MUD（房間 + 出口連接，無 2D 格子、無 Canvas、無像素計算）
- **介面參考**：指尖 MUD 風格——黑底彩字、可點擊按鈕取代指令輸入、手機友善

---

## 設計核心（不可違背）

- **從有限框架延伸出無限可能**
- 本質是將文字 MUD 放在手機上玩，省略一切輸入指令的過程（改以點擊、拖曳等操作）
- **後端唯一數據真理**：所有邏輯與狀態以後端為準，前端僅為顯示窗口
- **玩家 = NPC，NPC = 玩家**：同一套實體與行為模型
- **不可無中生有**：思維邏輯閉環，設計架構嚴謹
- **永序、守衡**：一切其來有自、因果相環
- **重體驗、輕操作；重文字、輕畫面**
- **深度沉浸**

---

## 已定案決策（不可推翻，除非主管在專門對話中正式決斷）

### 決策 001：戰鬥規則統一
- 玩家 vs NPC、NPC vs NPC、玩家 vs 玩家皆同一套規則
- 戰鬥全自動、文字 log
- 勝負靠裝備、技能等後端屬性配置（田忌賽馬）

### 決策 002：插頭／插座語義
- 可互動對象（名詞）= 邏輯插座（可執行動作清單）
- 玩家／NPC 意圖 = 動詞（插頭）
- 後端依「插頭—插座匹配」判斷可執行性並執行

### 決策 004：技術棧與架構
- Go 伺服器、網頁前端、SQLite
- 九萬 NPC 惰性求值（延遲坍縮）：平時不跑，只存狀態 + 上次觀測時間
- 觀測時回推：從事件日誌回推 NPC 當前狀態
- 視野內才即時模擬
- 全域系統（經濟/政治/生態引擎）在獨立 goroutine 中運行

### 已廢棄：決策 003（城鎮即大地圖 2D 格點）
- 已改為純文字房間制 MUD，不再使用 2D 格子地圖

---

## 核心系統概要

### 詞盤系統
- token → 詞元、鎂（單一通用貨幣）、剝名、內盤／竅穴、三層詞盤
- 鎂產消閉環、竅穴／詞元對照表、語意邊界
- 詞盤見 reference/詞盤彙整.md；經濟見 reference/經濟彙整.md

### 人物屬性
- A+B 方案（詞盤驅動 + 雙軌）
- 基礎 3 項：體質／氣脈／靈敏
- 出身 × 念紋加權制（出身小、念紋大）
- 屬性見 reference/人物屬性彙整.md

### NPC 觀測與坍縮
- 玩家視線未及或下線後，NPC 處於多重疊加態故事線
- 玩家觀測到時，坍縮成現在態單一故事線
- 玩家實際接觸時，連過去態都坍縮
- 玩家離開視線或離線，NPC 恢復多重疊加態

### 經濟
- 鎂 = 單一通用貨幣，產消閉環
- 裝備附魔／強化為核心消耗
- NPC 驅動物價，玩家為參與者
- 時間綁定產出防無限刷

---

## Go 程式碼風格（必須遵守）

- 每個函數開頭必須有中文註解：做什麼、吃什麼參數、回傳什麼、副作用
- 變數命名用英文，關鍵業務邏輯旁附中文行內註解
- 一個函數不超過 50 行，超過就拆
- 禁止巢狀超過 3 層的 if/for
- 不用 interface 除非有兩個以上的實作
- 禁止 init() 函數
- 所有錯誤必須明確處理，禁止 `_ = someFunc()`
- 每個 .go 檔案開頭須有檔案級中文註解
- 一個檔案只做一件事，不超過 300 行

---

## 禁止事項（零容忍）

- 禁止產生框架式空殼程式碼（不可生成 TODO 或 placeholder）
- 禁止未經討論就改變資料結構
- 禁止硬編碼遊戲數值，可調參數必須集中管理
- 禁止省略錯誤處理
- 禁止自行引入新依賴（須先提出理由供主管決斷）
- 禁止自行刪除或重構既有程式碼（須先說明理由）
- 禁止任何與 Canvas、2D 格子地圖、像素計算相關的程式碼
- 禁止未經主管同意推翻已定案決策

---

## 協作流程

- 寫新模組前，先用中文列出設計方案，經主管同意後才寫程式碼
- 每次提交的程式碼必須能編譯通過
- 遇到設計抉擇時，提供至少兩個方案及利弊，由主管選擇
- 修改任何檔案前，先說明：要改哪個檔案的哪個函數、為什麼改、改完預期行為是什麼

---

## 前端版次與快取（必須遵守）

修改 `web/` 下會影響前端的檔案後，**必須**在 `web/index.html` 中將對應的 `?v=` 版次遞增，否則用戶端（含手機）可能繼續使用舊快取，除錯時易誤判為「改動沒生效」。

- 改 `web/main.js` → 遞增 `main.js?v=8.12` 的版次（如 8.13）
- 改 `web/mud-text.js` → 遞增 `mud-text.js?v=3.9`（如 3.10）
- 改 `web/ui.js` → 遞增 `ui.js?v=3.9`（如 3.10）
- 改 `web/*.css` 或樣式 → 若有 link 帶 `?v=`，則遞增該參數
- 改 `web/sw.js` → 遞增 `sw.js?v=4`（整數 +1，如 5）

操作：改完腳本／樣式後，打開 `web/index.html`，找到對應的 `<script src="...?v=X">` 或 `<link ...?v=X">`，將數字遞增後儲存。

---

## 世界觀（撰寫文檔／敘事前必讀）

撰寫任何**劇情、NPC 對白、系統日誌或專案文檔**前，必須先閱讀 **reference/世界觀：Token降維與生命演化.md**。敘事須符合該文件定調（Token 本質、同頻相斥、止念、降維、飛升者等），不得文不對題或擅自詮釋擴充。

---

## 文件體系（供查閱）

- COLLABORATION.md — 協作約定
- 技術約束規則.md — 程式碼規範
- **reference/世界觀：Token降維與生命演化.md** — 敘事層必讀（見上）
- DESIGN_KEYWORDS.md — 設計關鍵字全覽
- decisions/ — 已定案決策（001 戰鬥、002 插座、004 技術棧）
- reference/詞盤彙整.md — 詞盤系統（世界觀＋後端細規）；reference/經濟彙整.md — 經濟設定與規劃
- reference/人物屬性彙整.md — 屬性公式與 A+B 架構
- reference/人物角色模板.md — 實體結構
- 第一版可做清單.md — MVP 範圍

AI_CONTEXT.md
