# SoulSeed 模擬（創角展開三軸＋760 邊權）

## 執行方式

**在專案根目錄**（`singularity_world/`）：
```bash
go run ./cmd/soulseed_demo/main.go
```

**或先進入本目錄**（`cmd/soulseed_demo/`）：
```bash
go run .
# 或
go run main.go
```

固定 seed 重現同一角色：
```bash
go run . -seed=8583229252685345714
```
