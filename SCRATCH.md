# 討論便條（SCRATCH）

> **SCRATCH = 討論清單**（未收斂的辯證、腦迴路、分支、被糾正的錯法）
> **WORKBOARD = 工作清單**（已拍板的執行項、進度、技術決策）
> 兩者本質不同——別把討論過程寫進工作板，也別把執行任務寫進便條。
>
> context 壓縮後，只讀這份 + WORKBOARD.md 就能重建當前對話脈絡。
>
> 規則：
> - 拍板的 → 移到 WORKBOARD.md 或 wiki，從這裡刪掉
> - 淘汰的 → 畫線保留一行（考古用），不要整段刪
> - 分岔太多 → 頂端「當前焦點」只留一個
> - **用戶不發話我也要自主寫**，不問「要不要記 SCRATCH」打斷他思路

---

## 背景常識（不需拍板）

- 玩家在當前格只能看到當前格內容——MUD 基本設定
- 地圖 UI 只顯示：當前格 + 已探索地形記憶（只有地形）+ 未探索（黑）
- NPC 在後端實體化但**不在大地圖顯現**（玩家看不到全景分布）
- NPC 進出當前格 → 文字事件（「A 從北邊走進來」）
- 觀景窗保留（上帝視角除錯，跟玩家視角解耦）

## 當前焦點

**野怪要不要實體化**（連帶：遭遇戰觸發方式）

---

## 這輪對話的分支（回到專案後至今）

### 已釐清但未落地

- **彈窗 vs 抽屜（UI 實作）**：代碼層完全不同機制。彈窗 modal 是 overlay 疊上去、抽屜 drawer 是 flex 擠進去。用戶設想一直是彈窗，抽屜是我誤推的。
  - 教訓已存 feedback + skill（`ask-vision-first`、`explain-tech-plainly`）
  - 抽屜 bug 機率高（跟「方向鍵永遠固定底部」硬約束打架）
  - 前端 UI 決定**延後再整體調整**

### 地圖架構（城鎮粒度）

- **已定方向**：大地圖一房一格，一格就是一個房間（地形描述 + 動作插座 + 在場者）
- **已定方向**：不做格內延伸（一格就是一格）
- **已定方向**：要更細 → 切換到獨立的區域地圖（兩層架構，不連續縮放）
- **待拍板**：哪些格子能切區域地圖？
  - A. 只有複雜地點（城鎮、村莊、大建築）
  - B. 每格都可以（世界生成成本炸）
  - C. 標記機制（有聚落/建築的才能進）

### 野怪實體 vs 虛擬

- **用戶拐彎問**：資源走「地形 + 動作」模式，野怪是不是也該虛擬化？
- **兩分支**：
  - A. 隨機遇敵（JRPG 古典，進格子擲骰）
  - B. 主動挑戰（地形有「狩獵」按鈕，像採集）
- **如果訂「只見當前格原則」** → 野怪實體不實體變不重要，玩家反正看不到全景

### NPC 可見性

- **用戶拍了**：NPC 不在大地圖顯現，玩家只看到當前格
- **推論**：
  - NPC 在後端**還是實體化**（有座標、會移動、需求系統、念場）
  - 玩家前端**看不到**全景 NPC 分布
  - NPC 出入當前格 → 文字事件（「A 從北邊走進來」）
  - 觀景窗保留（上帝視角、除錯用，跟玩家視角解耦）
- **這條跟野怪虛擬化對齊** → 促成「只見當前格原則」

### 文字敘事層原則（辯證中，未拍板）

用戶拋球：「必須文字化又要避免文字負擔」

**辯證過程**（保留腦迴路，還沒收斂）：

1. **我第一輪錯法**：列五層豐富度素材（地形變體/痕跡/時段氛圍/小事件/NPC 湧現）——五層全開 = 文字牆
2. **我第二輪錯法**：用 `> 看 > 聽 > 等` 動詞解鎖分層——用戶回「這節制版更出戲」
   - 診斷：出戲來源是**介面感暴露**，不是文字量。動詞指令讓玩家感到底下有選單，像 RPG
3. **我第三輪修正**：散文壓縮——一段連續敘事編織地形+痕跡+氛圍進動作，沒有指令解鎖
   - 範例給了「腳印斷在這，像是跳上馬走了。風把爭論聲帶遠了。」
4. **用戶點題（關鍵糾正）**：「到『像是跳上馬走了』恰到好處，點到為止」
   - 後面「風把爭論聲帶遠」是我**作文式前後呼應**——替玩家把場景寫完，搶走腦補空間

**抽出來的硬原則**：

> 寫到有想像空間就停。寫到沒想像空間就是作文。

每句寫完自問：「這句之後，玩家腦裡還剩問號嗎？」
- 還有問號 → 停
- 沒問號了 → 上一句就該收

**可能的實作影響（待具體化）**：

- 地形描述**不是** `template.format(terrain, weather, time)` 多欄位填值，是**敘事生成器**：吃 (地形, 時段, 天氣, 近期痕跡, 進格來向) 吐連續段落
- 訊息編織進動作（「走進」「泥還軟」「腳印斷在這」），不條列
- 跨格訊息延續要有**節制**——**不為連貫而連貫**，能幫玩家腦補的留下，替玩家寫完的砍掉
- 沉默是真沉默（不輸出），不是空行、不是「你繼續站在 X」狀態回報
- 每 tick 抽 0~1 層冒出來，多半抽 0

**仍未收斂的點**：

- 沉默期怎麼不讓玩家覺得當機？（考量：連續沉默幾 tick 後要不要被動提示？還是完全信任玩家？）
- delta 事件插入的文學化處理（例：鐘聲傳來）要跟進格描述同級文學密度嗎？還是更克制？
- 玩家自己的動作回饋（採集、移動）也套留白原則嗎？
- 描述池/敘事生成器的結構——模板化到什麼程度？模板太死就回到「作文老師」問題

---

## 待拍板（依序）

1. ⭐ 野怪實體 vs 虛擬 + 觸發模式（A 隨機 / B 主動）
2. 區域地圖入口方案（A 只有複雜地點 / B 每格皆可 / C 標記機制）
3. NPC 移動敘事呈現方式（進出通知 vs 即時串流）
4. 前端 UI 調整方向（等上面幾條清楚再回頭）

---

## 已拍板但未寫 wiki（需 design-discussion-close 處理）

（空）

---

## 淘汰線（考古用）

（空）

---

_最後更新：2026-04-16_
