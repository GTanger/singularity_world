# 決策 010：後端採用 Rust

## 狀態

**已定案並落地**：伺服器與遊戲邏輯以 **Rust** 實作（Axum、Tokio、`src/` 模組結構）。

## 要旨（精簡）

- **型別與契約**：以 `cargo check`／`clippy` 將「可違反的慣例」收斂成編譯期與靜態檢查，降低代理實作時的隱性錯誤面。
- **舊版對照**：若文件仍出現歷史檔名，一律視為存檔語境；權威為目前 `src/**/*.rs`。

原長文（語言比較、行數統計、遷移步驟）已併入本決策精簡敘述，不再維護重複副本。
