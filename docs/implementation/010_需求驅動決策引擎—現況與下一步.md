# 需求驅動決策引擎（10.21）— 現況與下一步

> 對齊 [討論 002：NPC 需求驅動與求職機制](../discussions/002_NPC需求驅動與求職機制.md)、[奇點決策引擎架構](../reference/奇點決策引擎架構.md)（若存在）、`npc/decision.go`。

---

## 已存在（不必從零造輪）

| 模組 | 說明 |
|------|------|
| **`npc/decision.go` `Decide()`** | V2 加權候選：求職、乞討、採集、社交、閒逛、有職時額外採集／上班等 |
| **`BuildDecisionState` / `BuildDecisionContext`** | 鎂、指派、性格、房間 tag、場所／社交／荒野在範圍內房間 |
| **`RunJobMatchingTick`**（`npc/job_matching.go`） | 每 30s 撮合（與腦分離，補位入職） |
| **`DeductDailyExpense`** | 每遊戲日扣 NPC 食宿鎂（`db/npc_expense.go`），驅動生存緊迫度 |
| **`TravelerManager` + `MoveBrain`**（`npc/movement.go`） | `Decide` → `ResolveBrainPath` → 移動 → 到達敘事 → **`applyBrainArrivalEffects`**（`main.go`） |

---

## 本次補齊（選項 1 第一刀）

| 項目 | 說明 |
|------|------|
| **`IntentTrade` 進入 `Decide()`** | 背包有貨（`InventoryTotalQty`）且生存緊迫、且可達 **gate／social** 時，與乞討／採集競爭權重；膽量高略偏兜售 |
| **`ResolveBrainPath`：Trade** | 尋最近 **social** 或 **gate** |
| **到達效果** | `db.NpcStreetSellOne`：賣出第一筆物品 1 件、加鎂、記 `EvtTrade`、`DispTrade` |
| **出發敘事** | `buildDepartureNarrative` 補「找買家」 |

仍**未**做：狩獵專用意圖、與玩家／NPC 真實 **Trade 插座**對接、依物品表定價、離職跳槽進階。

---

## 建議下一步（優先序）

1. **試玩觀測**：讓 NPC 先 **Gather** 掙到草料、鎂低於 50 後，是否出現 **Trade** 意圖與兜售敘事。  
2. **定價**：`NpcStreetSellOne` 改為讀 `items` 或 store 單價，避免全用隨機鎂。  
3. **狩獵**：新增 `IntentHunt` 或沿用 **Slay** 插座 + 目標房間有敵對實體時加權（需戰鬥與掉落已對齊）。  
4. **與 10.20 銜接**：鎂與行為進 **傳聞／NPC 對話** 上下文（已由 autoresearch 管線帶鎂壓力敘事，可再加經濟 key）。

---

*本檔隨 10.21 迭代更新。*
