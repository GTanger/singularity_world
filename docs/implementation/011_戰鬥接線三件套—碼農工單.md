# 011 — 戰鬥接線三件套（碼農工單）

> **你只需要照做，不要自己發明新東西。**
> 每一步都有完整代碼，複製貼上到指定位置，改完跑 `cargo build --release && cargo clippy -- -D warnings && cargo test`。
> **不要引入新 crate。不要改其他檔案。**

---

## 任務一：soul_seed → 戰鬥係數

### 步驟 1：在 `src/db/mod.rs` 新增函式

找到 `pub fn expand_soul_seed_to_personality` 函式（約第 216 行），在它**下方**貼上：

```rust
/// 由 soul_seed 展開為戰鬥三係數（能階α、時脈β、相位γ）。
pub fn expand_soul_seed_to_combat_axes(seed: i64) -> (f64, f64, f64) {
    let (amp, freq, phase) = expand_seed_axes(seed);
    let alpha = 0.5 + (amp - AMP_MIN) / (AMP_MAX - AMP_MIN) * 1.5;
    let beta = 0.5 + (freq - FREQ_MIN) / (FREQ_MAX - FREQ_MIN) * 1.0;
    let gamma = (phase - PHASE_MIN) / (PHASE_MAX - PHASE_MIN);
    (alpha, beta, gamma)
}
```

不需要改任何 import，函式內用到的 `expand_seed_axes`、`AMP_MIN` 等都在同一個檔案裡已有。

### 步驟 2：修改 `src/server/action.rs` 的 `build_attack_narrative` 函式

找到這個函式（約第 286 行），目前長這樣：

```rust
fn build_attack_narrative(
    room_id: &str,
    attacker: &Character,
    defender: &Character,
    subdue: bool,
) -> (String, String, i32, i32) {
    let mut opt = CombatOpt {
        subdue,
        ..Default::default()
    };
    let t = terrain_from_room(room_id);
    if !t.is_empty() {
        opt.terrain = t;
    }
    let (winner, raw_log, a_hp, d_hp) = resolve_v2(
```

在 `opt.terrain = t;` 那個 `}` 的**下一行**（`let (winner, raw_log` 之前），插入以下代碼：

```rust
    // 接入 soul_seed 戰鬥係數
    if let Some(seed) = attacker.soul_seed {
        let (a, _b, g) = db::expand_soul_seed_to_combat_axes(seed);
        opt.alpha = a;
        opt.gamma = g;
    }
    if let Some(seed) = defender.soul_seed {
        let (_a, _b, g) = db::expand_soul_seed_to_combat_axes(seed);
        opt.gamma = (opt.gamma + g) / 2.0;
    }
```

然後確認檔案頂部的 `use crate::db;` 或 `use crate::db::*;` import 已包含 `expand_soul_seed_to_combat_axes`。如果 import 寫法是 `use crate::db;` 就不用改。如果是 `use crate::db::{...}` 列舉式的，在列表裡加上 `expand_soul_seed_to_combat_axes`。

### 驗證

```bash
cargo build --release && cargo test
```

現有 4 個 combat test 應該全通過（因為 test 裡的 Character 沒有 soul_seed，走預設值不影響）。

---

## 任務二：命中/閃避公式

### 步驟 1：在 `src/combat/mod.rs` 新增函式

找到 `fn apply_phase`（約第 79 行），在它**下方**貼上：

```rust
/// 命中判定。回傳 true 表示命中。
fn hit_check<R: Rng + ?Sized>(rng: &mut R, atk_dex: i32, def_dex: i32) -> bool {
    let denom = (atk_dex + def_dex).max(1) as f64;
    let k_hit = atk_dex as f64 / denom;
    let p_hit = (k_hit * 1.2).clamp(0.05, 0.95);
    rng.random::<f64>() < p_hit
}
```

### 步驟 2：修改 `resolve_v2` 函式

`resolve_v2` 裡有 **4 處攻擊**需要加命中判定。以下逐一說明。

#### 位置 A：attacker_first = true，攻方打守方（約第 208-223 行）

**原本：**
```rust
        if attacker_first {
            let base = damage_v2(attacker_vit, defender_vit, opt);
            let (dmg, tag) = apply_phase(&mut rng, base, o.gamma);
            d_hp -= dmg;
            log.push_str(&format!(
                " [Action:Skill] 攻方擊中守方，傷害 {dmg}。{tag} [Result] 守方剩餘 {d_hp}。"
            ));
            if d_hp <= 0 {
```

**改為：**
```rust
        if attacker_first {
            if !hit_check(&mut rng, attacker_dex, defender_dex) {
                log.push_str(" [Action:Miss] 攻方揮空。");
            } else {
                let base = damage_v2(attacker_vit, defender_vit, opt);
                let (dmg, tag) = apply_phase(&mut rng, base, o.gamma);
                d_hp -= dmg;
                log.push_str(&format!(
                    " [Action:Skill] 攻方擊中守方，傷害 {dmg}。{tag} [Result] 守方剩餘 {d_hp}。"
                ));
                if d_hp <= 0 {
                    if subdue {
                        d_hp = 1;
                        log.push_str(" 守方氣竭倒地。 [Result] 攻方留人（制伏）。");
                        return ("attacker".to_string(), log, a_hp, d_hp);
                    }
                    log.push_str(" 守方敗。 [Result] 攻方取得了勝利。");
                    return ("attacker".to_string(), log, a_hp, d_hp);
                }
            }
```

#### 位置 B：attacker_first = true，守方反擊攻方（位置 A 之後）

**原本：**
```rust
            let base = damage_v2(defender_vit, attacker_vit, opt);
            let (dmg, tag) = apply_phase(&mut rng, base, o.gamma);
            a_hp -= dmg;
            log.push_str(&format!(
                " 守方反擊。 [Action:Skill] 守方擊中攻方，傷害 {dmg}。{tag} [Result] 攻方剩餘 {a_hp}。"
            ));
            if a_hp <= 0 {
```

**改為：**
```rust
            if !hit_check(&mut rng, defender_dex, attacker_dex) {
                log.push_str(" 守方反擊。 [Action:Miss] 守方揮空。");
            } else {
                let base = damage_v2(defender_vit, attacker_vit, opt);
                let (dmg, tag) = apply_phase(&mut rng, base, o.gamma);
                a_hp -= dmg;
                log.push_str(&format!(
                    " 守方反擊。 [Action:Skill] 守方擊中攻方，傷害 {dmg}。{tag} [Result] 攻方剩餘 {a_hp}。"
                ));
                if a_hp <= 0 {
                    if subdue {
                        a_hp = 1;
                        log.push_str(" 攻方不支倒地。 [Result] 守方留人（制伏）。");
                        return ("defender".to_string(), log, a_hp, d_hp);
                    }
                    log.push_str(" 攻方敗。 [Result] 守方取得了勝利。");
                    return ("defender".to_string(), log, a_hp, d_hp);
                }
            }
```

#### 位置 C：attacker_first = false，守方先打攻方（`} else {` 分支內）

**原本：**
```rust
        } else {
            let base = damage_v2(defender_vit, attacker_vit, opt);
            let (dmg, tag) = apply_phase(&mut rng, base, o.gamma);
            a_hp -= dmg;
            log.push_str(&format!(
                " [Action:Skill] 守方擊中攻方，傷害 {dmg}。{tag} [Result] 攻方剩餘 {a_hp}。"
            ));
            if a_hp <= 0 {
```

**改為：**
```rust
        } else {
            if !hit_check(&mut rng, defender_dex, attacker_dex) {
                log.push_str(" [Action:Miss] 守方揮空。");
            } else {
                let base = damage_v2(defender_vit, attacker_vit, opt);
                let (dmg, tag) = apply_phase(&mut rng, base, o.gamma);
                a_hp -= dmg;
                log.push_str(&format!(
                    " [Action:Skill] 守方擊中攻方，傷害 {dmg}。{tag} [Result] 攻方剩餘 {a_hp}。"
                ));
                if a_hp <= 0 {
                    if subdue {
                        a_hp = 1;
                        log.push_str(" 攻方不支倒地。 [Result] 守方留人（制伏）。");
                        return ("defender".to_string(), log, a_hp, d_hp);
                    }
                    log.push_str(" 攻方敗。 [Result] 守方取得了勝利。");
                    return ("defender".to_string(), log, a_hp, d_hp);
                }
            }
```

#### 位置 D：attacker_first = false，攻方反擊守方（位置 C 之後）

**原本：**
```rust
            let base = damage_v2(attacker_vit, defender_vit, opt);
            let (dmg, tag) = apply_phase(&mut rng, base, o.gamma);
            d_hp -= dmg;
            log.push_str(&format!(
                " 攻方反擊。 [Action:Skill] 攻方擊中守方，傷害 {dmg}。{tag} [Result] 守方剩餘 {d_hp}。"
            ));
            if d_hp <= 0 {
```

**改為：**
```rust
            if !hit_check(&mut rng, attacker_dex, defender_dex) {
                log.push_str(" 攻方反擊。 [Action:Miss] 攻方揮空。");
            } else {
                let base = damage_v2(attacker_vit, defender_vit, opt);
                let (dmg, tag) = apply_phase(&mut rng, base, o.gamma);
                d_hp -= dmg;
                log.push_str(&format!(
                    " 攻方反擊。 [Action:Skill] 攻方擊中守方，傷害 {dmg}。{tag} [Result] 守方剩餘 {d_hp}。"
                ));
                if d_hp <= 0 {
                    if subdue {
                        d_hp = 1;
                        log.push_str(" 守方氣竭倒地。 [Result] 攻方留人（制伏）。");
                        return ("attacker".to_string(), log, a_hp, d_hp);
                    }
                    log.push_str(" 守方敗。 [Result] 攻方取得了勝利。");
                    return ("attacker".to_string(), log, a_hp, d_hp);
                }
            }
```

### 步驟 3：修改測試

因為加了命中率，原本的確定性測試會不穩定。修改 `src/combat/mod.rs` 底部的 tests：

**刪除** `resolve_v2_nil_matches_resolve_outcome` 整個 test，**替換為**：

```rust
    #[test]
    fn resolve_v2_nil_attacker_wins_majority() {
        // 攻方 vit=8 dex=7 vs 守方 vit=4 dex=3，攻方應該大部分時候贏
        let mut attacker_wins = 0;
        for _ in 0..200 {
            let (w, _, _, _) = resolve_v2(8, 7, 4, 3, None);
            if w == "attacker" {
                attacker_wins += 1;
            }
        }
        // 攻方優勢明顯，應贏超過 60%
        assert!(attacker_wins > 120, "attacker 只贏了 {attacker_wins}/200 次，太少");
    }

    #[test]
    fn dex_advantage_wins_more_often() {
        // 體質相同，dex 差距大
        let mut high_dex_wins = 0;
        for _ in 0..500 {
            let (w, _, _, _) = resolve_v2(10, 15, 10, 3, None);
            if w == "attacker" {
                high_dex_wins += 1;
            }
        }
        // 高 dex 方命中率更高，應贏超過 55%
        assert!(high_dex_wins > 275, "高 dex 方只贏了 {high_dex_wins}/500 次");
    }
```

**修改** `subdue_sets_loser_hp_to_one`，把 alpha 設更高確保快速結束：

```rust
    #[test]
    fn subdue_sets_loser_hp_to_one() {
        let opt = CombatOpt {
            alpha: 200.0,
            subdue: true,
            ..Default::default()
        };
        // 跑多次取第一次有結果的
        for _ in 0..50 {
            let (w, _log, a, d) = resolve_v2(50, 10, 5, 5, Some(&opt));
            if w == "attacker" && d == 1 {
                assert!(a > 0);
                return;
            }
        }
        panic!("50 次都沒出現 attacker 制伏成功");
    }
```

`damage_first_stage_matches_go_edge` 和 `resolve_v2_alpha_terrain_log_tags` **不用改**。

### 驗證

```bash
cargo build --release && cargo clippy -- -D warnings && cargo test
```

---

## 任務三：裝備屬性欄位

### 步驟 1：確認物品結構體

找到物品定義的結構體（在 `src/store/mod.rs` 或 `src/db/` 中搜尋 `struct Item` 或搜尋 `items.json` 的讀取邏輯）。在結構體中加三個欄位：

```rust
    #[serde(default)]
    pub vit_bonus: i32,
    #[serde(default)]
    pub dex_bonus: i32,
    #[serde(default)]
    pub atk_bonus: i32,
```

`#[serde(default)]` 確保舊的 JSON 不會解析失敗（缺欄位時自動填 0）。

### 步驟 2：在 `src/db/mod.rs` 新增函式

在 `expand_soul_seed_to_combat_axes` 下方貼上：

```rust
/// 計算角色穿戴裝備後的有效屬性 (e_vit, e_qi, e_dex, atk_gear)。
pub fn effective_stats(ch: &crate::entity::Character) -> (i32, i32, i32, i32) {
    let mut e_vit = ch.vit;
    let mut e_dex = ch.dex;
    let mut atk_gear: i32 = 0;
    if !ch.equipment_slots.is_empty() {
        if let Ok(slots) = serde_json::from_str::<std::collections::HashMap<String, String>>(&ch.equipment_slots) {
            for (_slot, item_id) in &slots {
                if item_id.is_empty() {
                    continue;
                }
                if let Some(item_def) = get_item_def(item_id) {
                    e_vit += item_def.vit_bonus;
                    e_dex += item_def.dex_bonus;
                    atk_gear += item_def.atk_bonus;
                }
            }
        }
    }
    (e_vit, ch.qi, e_dex, atk_gear)
}
```

確認 `get_item_def` 函式在同檔案或同模組可存取。如果 `get_item_def` 回傳的型別沒有 `vit_bonus` 等欄位，要回步驟 1 確認結構體有加。

### 步驟 3：修改 `build_attack_narrative`

在 `src/server/action.rs` 的 `build_attack_narrative` 函式中，找到呼叫 `resolve_v2` 的地方：

```rust
    let (winner, raw_log, a_hp, d_hp) = resolve_v2(
        attacker.vit,
        attacker.dex,
        defender.vit,
        defender.dex,
        Some(&opt),
    );
```

**改為：**

```rust
    let (a_evit, _, a_edex, _a_gear) = db::effective_stats(attacker);
    let (d_evit, _, d_edex, _d_gear) = db::effective_stats(defender);
    let (winner, raw_log, a_hp, d_hp) = resolve_v2(
        a_evit,
        a_edex,
        d_evit,
        d_edex,
        Some(&opt),
    );
```

確認 import 有包含 `effective_stats`（如果用 `db::effective_stats` 呼叫就不用額外 import）。

### 步驟 4：items.json 加欄位（選做）

目前不急。框架能編譯通過就好。之後再逐步給裝備加非零的 bonus 值。
如果想測試，找 `data/items.json` 中任意一個裝備類物品，加上：

```json
"vit_bonus": 1,
"dex_bonus": 1,
"atk_bonus": 0
```

### 驗證

```bash
cargo build --release && cargo clippy -- -D warnings && cargo test
```

---

## 完工檢查清單

- [ ] `cargo build --release` 編譯通過
- [ ] `cargo clippy -- -D warnings` 零警告
- [ ] `cargo test` 全部通過
- [ ] 沒有新增任何 crate 依賴
- [ ] 只改了以下檔案：
  - `src/db/mod.rs`
  - `src/combat/mod.rs`
  - `src/server/action.rs`
  - 物品結構體所在檔案（加 3 個 serde default 欄位）
  - `data/items.json`（選做）
