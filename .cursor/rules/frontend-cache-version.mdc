---
description: 更新 web 前端代碼後須遞增 index.html 中的版次 (?v=) 以強制快取更新
globs: web/**/*
alwaysApply: false
---

# 前端版次與快取更新

當你**修改**以下任一檔案時，必須在 `web/index.html` 中將對應的版次參數遞增，否則用戶端（含手機）可能繼續使用舊快取。

## 須同步遞增版次的對照

| 修改的檔案 | 在 index.html 中更新 |
|------------|----------------------|
| `web/main.js` | `main.js?v=8.12` → 小數點後 +1（如 `8.13`） |
| `web/mud-text.js` | `mud-text.js?v=3.9` → 小數點後 +1（如 `3.10`） |
| `web/ui.js` | `ui.js?v=3.9` → 小數點後 +1（如 `3.10`） |
| `web/*.css` 或樣式 | 若有 link 帶 `?v=`，則遞增該參數 |
| `web/sw.js` | `sw.js?v=4` → 整數 +1（如 `5`） |

## 操作步驟

1. 完成對 `web/` 下腳本或樣式的修改。
2. 打開 `web/index.html`，找到對應的 `<script src="...?v=X">` 或 `<link ...?v=X">`。
3. 將 `?v=` 後的數字遞增（主版本.次版本 只改次版本即可，如 8.12 → 8.13）。
4. 儲存。之後部署或重新整理即可讓客戶端拉取新檔。

未遞增版次會導致手機或瀏覽器持續使用舊版 JS/CSS，除錯時容易誤判為「改動沒生效」。
